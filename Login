import unittest
import time
from selenium import webdriver
from webdriver_manager.chrome import ChromeDriverManager
from selenium.webdriver.common.keys import Keys
from selenium.webdriver.common.by import By
from selenium.webdriver.support.wait import WebDriverWait

class TestDaftar(unittest.TestCase): # test scenario

    def setUp(self):
        self.browser = webdriver.Chrome(ChromeDriverManager().install())
    
    def test_success_login(self): #test cases 1
        driver = self.browser
        driver.get("https://barru.pythonanywhere.com/daftar")
        driver.find_element(By.ID, "email").send_keys("sanbercode@test.com")
        driver.find_element(By.ID, "password").send_keys("12345678")
        driver.find_element(By.ID, "signin_login").click()

    #def test_failed_login(self): #test cases 2
        #driver = self.browser
        #driver.get("https://barru.pythonanywhere.com/daftar")
        #driver.find_element(By.ID, "email").send_keys("sanbercode@test.com")
        #driver.find_element(By.ID, "signin_login").click()
        #error_message = driver.find_element(By.NAME,"swal2-content").text
        #self.assertIn("User's not found", error_message)
        #driver.find_element(By.CLASS_NAME, "swal2-confirm swal2-styled").click()
   
if __name__ == '__main__':
    unittest.main()